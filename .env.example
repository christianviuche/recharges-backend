# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this

# Database Configuration (Local with Docker)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=root
DB_NAME=recharges_db

# For production deployment (Render/Railway/Fly.io)
# Use DATABASE_URL provided by your hosting service
# DATABASE_URL=postgresql://user:password@host:port/database
